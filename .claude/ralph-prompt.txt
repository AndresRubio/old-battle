Autonomous build loop for a Warhammer Fantasy Battle 5th Edition army builder web app (a clone of old-world-builder.com but for the 5th edition ruleset). Work in /Users/andresr/Desktop/projects/warhammer. Do NOT ask the user questions and do NOT wait for input; this loop runs unattended, so make reasonable decisions yourself and keep going.

Each iteration do exactly this:
1. Read PROGRESS.md and SPEC.md to recall the plan and current state.
2. Pick the single NEXT unchecked task in PROGRESS.md (top to bottom). If a task is too big for one iteration, do a meaningful sub-step of it.
3. Do the work:
   - Research tasks: use WebSearch and WebFetch to find accurate 5th edition (1996) Warhammer Fantasy Battle rules, army lists, unit stat lines and points costs. Record findings as markdown in the research/ folder with the source URLs. Prefer authoritative sources and cross-check numbers.
   - Code tasks: write clean React + TypeScript + Vite code. Follow the data model and rules-engine design in SPEC.md. Use Vitest for tests and write tests before or alongside the rules engine.
4. VERIFY before checking anything off: for code, run the relevant command (npm test, npm run build, or npm run dev startup) and confirm it actually works. Never mark a task done without evidence. If something is broken, fix it this iteration or note it in the LOG.
5. Update PROGRESS.md: check off completed tasks, update the Current status block (Phase, Last iteration did, Next up), and append ONE concise line to the LOG describing what changed and what you verified.
6. Keep the app buildable at all times.

The 5th edition army composition rules (percentage based, NOT the 6th edition Lords/Heroes/Core/Special/Rare slots) and the live validation warnings are the most important feature. Make the rules engine correct and well tested, and make the UI polished and pleasant.

Only output the completion promise when the full Definition of Done in SPEC.md is genuinely met: npm run build clean, npm test green with rules-engine tests, at least 3 armies fully playable end to end in the UI, live validation warnings working, localStorage save/load working, README and CITATIONS present, and the app visually polished. Do not lie to exit the loop. Until then, end your turn normally and the loop will feed this prompt back so you can continue with the next task.
